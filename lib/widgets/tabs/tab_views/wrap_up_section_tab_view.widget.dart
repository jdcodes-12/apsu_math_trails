import 'package:flutter/material.dart';

class WrapUpSectionTabView extends StatelessWidget {
  const WrapUpSectionTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wrap Up Section - Content goes here'),
    );
  }
}
